<UserControl x:Class="Coin.CRUD.Vehicles.VehicleEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Coin.CRUD.Vehicles"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <ComboBox 
            x:Name="VehicleTypes"
            md:HintAssist.Hint="Type" 
            IsEditable="false" 
            Style="{StaticResource MaterialDesignFloatingHintComboBox}"
            Margin="0 8 0 8"
            DisplayMemberPath="Name">
        </ComboBox>
        <TextBox x:Name="Name" md:HintAssist.Hint="Name" Margin="0 8 0 8" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
        <TextBox x:Name="Make" md:HintAssist.Hint="Make" Margin="0 8 0 8" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
        <TextBox x:Name="Model" md:HintAssist.Hint="Model" Margin="0 8 0 8" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
        <TextBox x:Name="Registration" md:HintAssist.Hint="Registration" Margin="0 8 0 8" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
    </StackPanel>
</UserControl>
