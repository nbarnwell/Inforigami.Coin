<UserControl x:Class="Coin.Transactions.AccountStatementWithTransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Coin.Transactions"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <WrapPanel DockPanel.Dock="Top">
            <ContentControl Name="Account" cal:View.Context="Detail" Margin="8"></ContentControl>
            <ContentControl Name="Statement" cal:View.Context="Detail" Margin="8"></ContentControl>
        </WrapPanel>
        <DataGrid 
            Name="Transactions"
            AutoGenerateColumns="False" 
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Period Start" Binding="{Binding PeriodStart, StringFormat={}{0:Y}}" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
