<UserControl x:Class="Coin.Transactions.AccountStatementListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Coin.Transactions"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <ContentControl x:Name="AccountDetails" DockPanel.Dock="Top" cal:View.Context="Detail"></ContentControl>
        <ItemsControl x:Name="Statements">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Style="{StaticResource FlatButton}" cal:Message.Attach="ShowStatement($dataContext)" cal:View.Model="{Binding}" cal:View.Context="ListItem" Margin="16">
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</UserControl>
