<UserControl x:Class="Coin.Shared.MoneyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Coin.Shared"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="White">
    <DockPanel>
        <TextBlock DockPanel.Dock="Right" Text="{Binding Currency.Code}" Margin="8 24 0 8" materialDesign:HintAssist.Hint="Currency" Style="{StaticResource MaterialDesignBody1TextBlock}"></TextBlock>
        <TextBox Name="AmountInput" Margin="0 8 0 8" TextAlignment="Right" materialDesign:HintAssist.Hint="Amount" Style="{StaticResource MaterialDesignFloatingHintTextBox}"></TextBox>
    </DockPanel>

</UserControl>
