<UserControl x:Class="Coin.Shared.DialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Coin.Shared"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel Margin="16">
        <WrapPanel DockPanel.Dock="Bottom">
            <Button x:Name="Accept" Style="{StaticResource MaterialDesignFlatButton}">ACCEPT</Button>
            <Button x:Name="Cancel" Style="{StaticResource MaterialDesignFlatButton}">CANCEL</Button>
        </WrapPanel>
        <ContentControl x:Name="Content"></ContentControl>
    </DockPanel>
</UserControl>
