@page
@model Coin.Web.Areas.Vehicles.Pages.VehicleTypes.IndexModel

@{
    ViewData["Title"] = "VehicleType Index";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VehicleType[0].Name)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.VehicleType) {
            <tr class="cursor-hand" data-href="@Url.Page("Details", new { id = item.Id })">
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
        </tr>
}
    </tbody>
</table>
