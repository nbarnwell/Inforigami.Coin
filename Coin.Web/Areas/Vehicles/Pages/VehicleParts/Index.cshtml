@page
@model Coin.Web.Areas.Vehicles.Pages.VehicleParts.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VehiclePart[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VehiclePart[0].VehicleType)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.VehiclePart) {
            <tr class="cursor-hand" data-href="@Url.Page("Details", new { id = item.Id })">
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VehicleType.Name)
            </td>
        </tr>
}
    </tbody>
</table>
