@page "{id?}"
@model Coin.Web.Areas.Accounting.Pages.Budgets.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Budget</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Budget.Id)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Budget.Id)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Budget.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Budget.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Budget.Household)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Budget.Household.Name)
        </dd>
    </dl>
    <dl>
        <dt>See Also</dt>
        <dd>
            @Url.Page("Index") |
            @Url.Page("../Budgets/Index") |
            @Url.Page("../BudgetItems/Index") |
            @{ 
                var pluralisedEntityName = "BudgetItems";
                var areaName = "Accounting";
                var displayName = "Budget Items";
            <a href="@Url.Page(pluralisedEntityName + "/Index", new { Area = areaName, BudgetId = Model.Budget.Id })">@displayName</a>
                <a asp-page="../Accounting/BudgetItems/Index" asp-route-id="@Model.Budget.Id">@displayName</a>
            //<a href="@Url.Page("../Vehicles/Index", new { Area = "Vehicles", BudgetId = Model.Budget.Id })">Vehicles</a>
            //<a href="@Url.Page("../Households/Index", new { BudgetId = Model.Budget.Id })">Households</a>
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Budget.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
