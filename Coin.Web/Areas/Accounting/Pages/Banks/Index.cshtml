@page
@model Coin.Web.Areas.Accounting.Pages.Banks.IndexModel

@{
    ViewData["Title"] = "Bank Index";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bank[0].Name)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Bank) {
            <tr class="cursor-hand" data-href="@Url.Page("Details", new { id = item.Id })">
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
        </tr>
}
    </tbody>
</table>
