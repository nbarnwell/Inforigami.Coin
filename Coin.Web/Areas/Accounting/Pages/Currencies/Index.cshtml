@page
@model Coin.Web.Areas.Accounting.Pages.Currencies.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Currency[0].Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Currency[0].Name)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Currency) {
            <tr class="cursor-hand" data-href="@Url.Page("Details", new { id = item.Id })">
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
        </tr>
}
    </tbody>
</table>
